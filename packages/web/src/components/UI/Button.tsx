import styled from "styled-components";

const Button = styled.button.attrs({
  className: "text-blue-600 hover:bg-blue-100 cursor-pointer font-bold py-2 px-4 border border-blue-700 rounded"
})``;

export default Button;
