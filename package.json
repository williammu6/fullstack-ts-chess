{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run --parallel start",
    "start2": "yarn workspaces run start",
    "watch": "lerna run watch --stream",
    "tsc": "lerna run tsc --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^3.9.7"
  },
  "name": "fullstack-ts-chess"
}
